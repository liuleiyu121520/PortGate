# 本地打包环境变量模板（复制为 .env 使用；.env 不入库）
# 说明：npm run dist 会通过 node --env-file-if-exists=.env 自动加载本文件。

# electron-builder 拉取 Electron 发行包的镜像源（国内网络推荐；不设则走官方 GitHub Releases）
#ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/

# 固定未签名口径：本机存在证书时也不被自动签名（与 electron-builder.yml identity:null 双保险）
#CSC_IDENTITY_AUTO_DISCOVERY=false
