# PortGate
本机端口、进程、应用、项目之间的可视化监控与安全管理工具

## 本地开发

```bash
npm install
npm run dev
```

## 测试与检查

```bash
npm test            # vitest 单元测试（IPC 白名单 / 契约）
npm run lint        # ESLint（含 AC-16 边界守护规则）
npm run typecheck   # tsc / vue-tsc
```
